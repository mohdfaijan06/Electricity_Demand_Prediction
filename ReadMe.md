Project Setup Guide in VS Code:

1. Create new environment
python -m venv .venv # this will create new env with name .venv

2. Activate environment
.venv/Scripts/activate

3. Create new file requirements.txt and place your required packages there.

4. install packages in requirements.txt
pip install -r requirements.txt

